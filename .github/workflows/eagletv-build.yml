name: Build EagleTV APK

on:
  push:
    branches: [eagletv-beta]
    paths:
      - "EagleTV/**"
      - ".github/workflows/eagletv-build.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle
      - name: Build release APK
        run: |
          chmod +x gradlew
          ./gradlew -p EagleTV :app:assembleRelease --stacktrace
          cp EagleTV/app/build/outputs/apk/release/app-release.apk EagleTV.apk
      - uses: actions/upload-artifact@v4
        with:
          name: EagleTV-beta
          path: EagleTV.apk
          if-no-files-found: error
          retention-days: 30
